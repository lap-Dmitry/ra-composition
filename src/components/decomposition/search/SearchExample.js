// Показывает пример поиска

export default function SearchExample() {
    return (
        <div className="search_example">
            <span> Найдётся всё. Например, </span>
            <a href="/some/valid/uri" className="search_example__link">фаза луны сегодня</a>
        </div>
    );
}
